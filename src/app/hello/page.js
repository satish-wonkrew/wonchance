import React from "react";
import LiveCamComponent from "../userForm/LiveCamComponent";

const page = () => {
  return (
    <div>
      <LiveCamComponent />
    </div>
  );
};

export default page;
